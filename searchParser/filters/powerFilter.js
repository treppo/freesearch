module.exports = function () {
    'use strict';

    var _powerMarker = ['ps', 'kw'];

    var filter = function(searchTokens) {
        return searchTokens;
    };

    return filter;
};
