module.exports = function () {
    'use strict';
    
    var _models = ['a1', 'a3', 'a4']
    
    var filter = function(tokens) {
        console.log('model filter does ');
        return tokens;
    };

    return filter;
};