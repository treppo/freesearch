module.exports = function () {
    'use strict';
    
    var filter = function(tokens) {
        //console.log('none filter does something');
        return tokens;
    };

    return filter;
};