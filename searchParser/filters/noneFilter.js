module.exports = function () {
    'use strict';

    var filter = function (searchTokens) {
        //console.log('none filter does something');
        return searchTokens;
    };

    return filter;
};